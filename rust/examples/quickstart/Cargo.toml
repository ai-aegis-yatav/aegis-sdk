[package]
name = "aegis-quickstart"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
aegis-sdk = { path = "../.." }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
