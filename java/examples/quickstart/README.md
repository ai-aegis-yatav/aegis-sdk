# AEGIS Java Quickstart

```bash
export AEGIS_API_KEY=aegis_sk_...
bash run.sh
```

Or from repo root: `make sdk-quickstart-java`.

Uses JitPack by default. For local development, uncomment the
`includeBuild("../..")` line in `settings.gradle.kts`.
